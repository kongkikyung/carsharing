<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sql">
    <select id="selectUserAll" resultType="vo.UserVO">
        select user_id, email, password, name, department, created_datetime, updated_datetime
        from user
    </select>
</mapper>